/** ES5 */
let multiply = function(a,b){

    return a*b;
}

/** ES6 */

multiply = (a,b) =>{
    return a*b;
}

multiply = (a,b) => a*b;

let double = number => number*2;

let doubledNumbers = numbers.map((number) => {
    return 2*number;
});

doubleNumber = numbers.map(number=> 2*number );